// Generated by CIRCT firtool-1.76.0
module Queue42_Tag(
  input  wire       clock,
  input  wire       reset,
  output wire       io_enq_ready,
  input  wire       io_enq_valid,
  input  wire [6:0] io_enq_bits_tag,
  input  wire       io_deq_ready,
  output wire       io_deq_valid,
  output wire [6:0] io_deq_bits_tag
);

  wire [6:0] _ram_tag_ext_R0_data;
  reg  [5:0] enq_ptr_value;
  reg  [5:0] deq_ptr_value;
  reg        maybe_full;
  wire       ptr_match = enq_ptr_value == deq_ptr_value;
  wire       empty = ptr_match & ~maybe_full;
  wire       full = ptr_match & maybe_full;
  wire       io_deq_valid_0 = io_enq_valid | ~empty;
  wire       do_deq = ~empty & io_deq_ready & io_deq_valid_0;
  wire       do_enq = ~(empty & io_deq_ready) & ~full & io_enq_valid;
  always @(posedge clock) begin
    if (reset) begin
      enq_ptr_value <= 6'h0;
      deq_ptr_value <= 6'h0;
      maybe_full <= 1'h0;
    end
    else begin
      if (do_enq)
        enq_ptr_value <= enq_ptr_value == 6'h29 ? 6'h0 : enq_ptr_value + 6'h1;
      if (do_deq)
        deq_ptr_value <= deq_ptr_value == 6'h29 ? 6'h0 : deq_ptr_value + 6'h1;
      if (~(do_enq == do_deq))
        maybe_full <= do_enq;
    end
  end // always @(posedge)
  ram_tag_42x7 ram_tag_ext (
    .R0_addr (deq_ptr_value),
    .R0_en   (1'h1),
    .R0_clk  (clock),
    .R0_data (_ram_tag_ext_R0_data),
    .W0_addr (enq_ptr_value),
    .W0_en   (do_enq),
    .W0_clk  (clock),
    .W0_data (io_enq_bits_tag)
  );
  assign io_enq_ready = ~full;
  assign io_deq_valid = io_deq_valid_0;
  assign io_deq_bits_tag = empty ? io_enq_bits_tag : _ram_tag_ext_R0_data;
endmodule

